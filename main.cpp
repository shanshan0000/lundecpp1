#include <iostream>

int main() {
    const auto PI = 3.14159;
    int r;
    std::cout << "donnez le rayon entier d’un cercle:" << std::endl << 10 << 100;
    std::cout.operator<<(8).operator<<(44).operator<<(66);
    std::cin >> r;
    auto p = 2 * PI * r;
    auto s = PI * r * r;
    s = p + 9;
    std::cout << "le cercle de rayon " << r;
    return 0;
}
