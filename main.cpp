#include "fraction.h"

// See the answer at Readme.md

int main(){
    using namespace MATH;
    Fraction f1(3,4);
    Fraction f2(5);

    // See the answer at Readme.md
    Fraction f4 = f1 + f2; // utilisation de la fonction
}